class Mom {
  Mom(this.dateTime, this.present, this.total);
  // date , start time, end time, duration, attendence , created by , content
  final DateTime dateTime;
  // final startime;
  // final endTime;
  // final duration = endTime - startTime;
  final int present;
  final int total;
}
